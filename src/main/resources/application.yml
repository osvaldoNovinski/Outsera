spring:
  application:
    name: Produtos API
  datasource:
    url: jdbc:h2:mem:outsera
    username: sa
    password: 1234
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enable: true
      path: /h2-console